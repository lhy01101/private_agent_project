# **💙方塘的优化建议💕~**
## **一、普通提问靠语义、日期提问靠规则+精确过滤，各用各擅长的武器。**
- ① 入库时——先拆标题，再打标签
- 不是按字数盲切，而是先解析 markdown 的 ## 标题，把"每个日期日志"当成一个完整条目，然后在 Chroma 里给它挂一个日期元数据：
-  解析 md，按 "## X月X日日志" 切分
- entries = split_by_markdown_headers(log.md)
-     for entry in entries:
-       chroma.add(
-           documents=[entry.text],
-           metadatas=[{"date": "2026-09-06"}],  # 从标题解析出来的精确日期
-       )
- ② 查询时——日期走精确过滤，不走语义
- 先拿个小规则（或正则）把主人问题里的"9月6日"解析出来，然后查 Chroma 时加上 where 条件：
-     if 检测到日期:
-         results = chroma.query(
-           query_texts=[问题去掉日期后的部分], 
-           where={"date": "2026-09-06"},   # 精确匹配，跟语义无关
-          )
-     else:
-         results = chroma.query(query_texts=[问题])  # 平时照旧
-   这样"查9月6日日志"就变成数据库精确筛选，一秒都花不了，也不会再捞出一堆 5日、7日、8月的东西来让方塘人工挑喵。
##  **二、“卡”就很可能出在这几处：**
- query 的 embedding 每次请求都现算一次。nomic-embed-text 如果没有常驻服务/缓存，每次请求都要加载模型再推理，这个开销比语义路由本身大得多。
- 向量检索本身要拿 query embed 和库里所有文档向量比对（Chroma 的查询操作），如果文档数量大、分块多，召回 top50 也会花时间——但一般来说本地 chroma 这个量级不至于秒级。
- cross_encoder 重排（rerank）是最可疑的慢点。因为它要拿 query 和 top50 条候选两两拼起来过一次 transformer，不像 bi-encoder 可以只算一次 query 向量。如果 rerank 模型不是 nomic，而是另一个模型（比如 bge-reranker），那还需要再加载一次模型。五十分之一秒级到秒级都可能是它。
- 所以方塘给主人的下一步建议是喵：
- 在代码里给链路分段打点（time.time() 或 perf_counter 日志打出来）：
- embed 耗时
- chroma.query 耗时
- rerank 耗时
- 看看具体是哪一段吃掉大头，再针对优化。

## **三、为什么添加了工具提示词的负例机制之后，调工具好像更快了？**
- Agent 工具调用加速：负例机制为什么有效

> 记录于 2026-09-10
> 背景：为工具提示词加入负例（不该调用的场景）后，工具调用体感明显变快。
> 结论：变快的不是"读提示词"，是"少跑了几轮"。

---

- 结论速览

| 机制 | 作用 |
| --- | --- |
| 降低假阳性调用 | 减少往返轮次（主要收益） |
| prefill 换 decode | 用便宜的可缓存 token 换掉昂贵的串行输出 |
| reasoning 缩短 | 同轮内思考链变短 |
| RAG 链路 | 一次误调 = 省下 50 条候选重排 |

---

1. 延迟的大头是轮次，不是 token 长度

Agent 的延迟结构：

```text
一轮对话      = 1 次 LLM 调用
调用一次工具  = +1 次 LLM 调用（模型看到结果要接着说话）
调错一次工具  = +2 次 LLM 调用（错调 + 纠错）
```

**最贵的成本是"往返次数"**，因为每一轮都是串行的，前一轮吐完才能跑下一轮。

负例做的事：把"这个场景不该调工具"的边界，从模型的**推理过程**里挪到**提示词**里。

- 模型不必自己判断"这算不算要联网"
- 直接照着边界走
- → 假阳性调用下降 → 轮次下降

这是主因，能占大半。

---

2. prefill 便宜，decode 贵

这个不对称才是关键：

|  | 输入 token（prefill） | 输出 token（decode） |
| --- | --- | --- |
| 计算方式 | 可并行，一次前向吃掉整段 | 串行，一次一个 token |
| 瓶颈 | 算力 | 显存带宽 |
| 单位成本 | 极低 | 高一个量级 |

负例让系统提示词长了大概一两百 token —— 这是 **prefill，便宜、还并行**。

省掉的却是一整轮 decode：那一轮光输出就好几百 token，后面还挂着一次工具执行。

再加上系统提示词是**前缀**，有 prompt caching / KV cache 兜着，第二通往后基本是白嫖。

> **用便宜的可缓存 token，换掉昂贵的串行往返——这笔账怎么算都赚。**

---

3. 推理链本身也短了

很多模型是同一轮里先出 reasoning 再出 tool_call。边界模糊时，它会先写一段：

```text
用户提到了X……我是否应该搜索……
```

然后才决定。

负例把这层权衡掐掉，reasoning 部分直接变短 → **首 token 后的 decode 量也降了**。

---

4. 对 RAG 这条路收益特别明显

当前链路：

```text
top50 召回 → cross_encoder 重排序 → top3
```

重排是本地跑的，还开了 HF 离线模式 —— 那部分开销是实打实的算力，不是网络等待。

**少一次误触发的 RAG，就是省下 50 条候选的重排。**

一次误调在这个架构里的代价比普通工具高得多，所以负例的边际收益也更大。

---

两个坑
坑二：负例写抽象了反而有害

```text
❌ 不要在不必要的时候调用工具
```

这种等于没写，只会给模型增加噪音。

**得贴着真实误触发的边界写**，最好直接搬那种"问过一次、结果调错了"的原句。

---

验证方法：埋三个点

| # | 指标 | 预期 |
| --- | --- | --- |
| 1 | 平均轮次 / 请求 | 降了就是真的 |
| 2 | tool_call 总数与误调用率 | 误调用率下降 |
| 3 | TTFT 与总耗时分开看 | TTFT 可能略升（提示词变长） |

**第 3 条最有意思：**

- TTFT 升了，但总耗时降了 → **完美印证 prefill 换 decode 这笔账**

---
一句话总结

> 负例不是让模型"看得更快"，
> 而是让它**一开始就走对路，少走一趟**。

---