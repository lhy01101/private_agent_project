# 文件管理架构设计文档

> 日期：2026-09-15  
> 状态：方向已确认，待工程落地  
> 作者：架构讨论记录

---

## 一、认知演进

```
最初：文件 = 硬路由问题，关键词检测 → 有文件就处理
  ↓
发现：文件应该区分大小，小文件直读、大文件检索
  ↓
进一步：文件不是"输入数据"，是"Agent 可管理的资源"
  ↓
最终：file_id + Tool 管理，Agent 主动决定何时查、查哪个
```

**核心跃迁**：从"把文件倒进 prompt"到"把文件注册为可访问资源"。

---

## 二、确认的设计决策

| # | 决策 | 结论 | 理由 |
|---|---|---|---|
| D1 | 存储位置 | 统一 Chroma，同实例 | 复用现有向量库，不引入新依赖 |
| D2 | 逻辑隔离 | metadata 带 `file_id` + `where` 过滤 | 文件间互不污染 |
| D3 | 原文落盘 | 不单独存，metadata 记 path 溯源 | 避免冗余存储 |
| D4 | 生命周期 | `temp`（会话级）/ `persistent`（显式保存） | 按需求分级 |
| D5 | 大小分流 | 小文件整篇直读 / 大文件切块入库 | 小文件零延迟，大文件可扩展 |
| D6 | 代码切块 | tree-sitter AST 按函数/类 | 精确到行号，定位而非概括 |
| D7 | 路由架构 | 三层：入口硬路由 → 软路由选 Tool → Tool 内部硬路由 | 各层职责清晰 |
| D8 | embedding | 与现有知识库同一模型（bge-m3） | 向量空间一致 |
| D9 | Agent 感知 | Tool 方案：file_id 注入对话历史 | LLM 知道资源存在、可反复访问 |

---

## 三、放弃的方案及理由

| 放弃的方案 | 为什么放弃 |
|---|---|
| 文件内容单独落盘 | metadata 记 path 即可溯源，不需要额外存储 |
| 查询翻译（中→英） | 有损 + 慢，bge-m3 原生支持中英混合 |
| 纯硬路由（关键词检测） | 长期对话中无法隐式参与，Agent 无法主动引用 |
| 每次都全量塞 prompt | 上下文窗口有限，长对话会丢失 |

---

## 四、方案对比：前置管线 vs Tool 方案

| 维度 | 前置管线（早期方案） | Tool 方案（当前选定） |
|---|---|---|
| 文件在 prompt 中的形态 | 匿名文本（一次性消费） | 有名字的资源（可反复查询） |
| LLM 是否知道文件存在 | 不知道（只看到内容） | 知道（file_id + 对话历史记录） |
| 长对话滚出窗口后 | 彻底丢失 | 可重新调 `query_file` |
| 隐式参考 | 做不到 | 能做到（Agent 主动关联） |
| 延迟 | 零额外 | 多一次 LLM 决策 |
| 确定性 | 高（代码控制） | 依赖 LLM 选对 Tool |

**结论**：前置管线适合临时对话、简单场景；Tool 方案适合长期 Agent、需要隐式参考的场景。当前选定 Tool 方案。

---

## 五、四层逻辑框架

```
┌─────────────────────────────────────────────────────────────────┐
│  Layer 0：入口硬路由（系统层）                                   │
│  · 检测消息是否含文件（附件 / file_id 引用 / 上传意图）            │
│  · 有文件 → 触发 ingest 管线                                     │
│  · 无文件 → 跳过                                                 │
└───────────────────────────┬─────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────────┐
│  Layer 1：文件处理管线（数据接入层）                               │
│  · Loader（按扩展名分发：PDF/PPTX/DOCX/代码）                     │
│  · 大小判断                                                      │
│    ├─ 小文件 → 全文缓存（inline_cache）                           │
│    └─ 大文件 → AST切块 / 语义切块 → 摘要生成                      │
│  · ingest_file Tool → 返回 file_id + 摘要                        │
│  · 文件列表注入 system prompt（让 Agent 知道有哪些资源）             │
└───────────────────────────┬─────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────────┐
│  Layer 2：语义路由（Agent 决策层）                                │
│  · query → embedding → 工具池（含文件 Tool）                      │
│  · Top-K 工具 → LLM 选哪个                                       │
│  · LLM 判断：需要查文件？→ 调 query_file(file_id, question)       │
│  · LLM 判断：不需要？→ 走其他 Tool 或直答                         │
└───────────────────────────┬─────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────────┐
│  Layer 3：Tool 执行（硬路由层）                                   │
│  · query_file 内部：                                              │
│    ├─ inline → 返回缓存全文                                       │
│    ├─ indexed → embedding query → Chroma where 过滤 → Top-K       │
│    ├─ rerank（可选）→ 返回最相关片段                               │
│    └─ file_id 归属校验（多用户隔离）                               │
│  · 结果返回给 LLM → 生成答案                                      │
└───────────────────────────┬─────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────────┐
│  Layer 4：生命周期管理（后台层，异步）                              │
│  · temp 文件：会话结束 / 超时 → cleanup_temp()                    │
│  · persistent 文件：显式保存 → 跨会话保留                          │
│  · 文件列表维护（增删改查）                                        │
└─────────────────────────────────────────────────────────────────┘
```

---

## 六、可能需要的新技术（按成熟度排序）

### 已成熟、可直接用

| 技术 | 用途 |
|---|---|
| tree-sitter | 代码 AST 切块 |
| bge-m3 | 中英混合 embedding |
| bge-reranker-v2-m3 | 检索后重排 |
| ChromaDB | 向量存储 |
| pdfplumber / python-pptx / python-docx | 文件解析 |

### 近期成熟、值得关注

| 技术 | 用途 | 引入时机 |
|---|---|---|
| 文件摘要模型（small LLM） | 入库时生成摘要，支撑隐式参考 | Agent 要做主动关联时 |
| BM25 + 向量混合检索 | 关键词 + 语义双路召回 | 文件量大、纯向量不够时 |
| LangGraph Send API | 动态扇出（多文件并行查询） | 多文件场景变多时 |
| 文件去重（simhash / minhash） | 用户重复上传同一文件 | 用户多了之后 |

### 前沿、未来可能用

| 技术 | 用途 |
|---|---|
| ColBERT / Late Interaction | 细粒度 token-level 检索 |
| 多模态 embedding（CLIP 类） | PPT 图表、PDF 图片检索 |
| Agent Memory（Mem0 / Letta） | 跨会话记忆管理 |
| MCP（Model Context Protocol） | 标准化 Tool 接口 |

---

## 七、隐式参考实现路径

```
Step 1：入库时生成文件摘要（1-3 句话 + 关键词）
  ↓
Step 2：system prompt 中注入文件列表
  "你有以下文件可用：
   - report.pdf (摘要：Q3 财报，含收入/支出/预测)
   - code.py (摘要：鉴权模块，含 verify_token/refresh)
   - data.csv (摘要：用户行为日志，10月-12月)"
  ↓
Step 3：用户提问时，Agent 内部判断
  "这个问题可能和 report.pdf 有关" → 调 query_file
  ↓
Step 4：即使你没提文件名，Agent 也能关联
```

---

## 八、下一步待办（按优先级）

| 优先级 | 事项 | 说明 |
|---|---|---|
| ⭐⭐⭐⭐⭐ | 文件列表注入 system prompt 的格式设计 | 让 Agent 知道有哪些资源但不浪费 token |
| ⭐⭐⭐⭐ | `ingest_file` / `query_file` 接口定义 + 实现 brief | 交给工程师落地 |
| ⭐⭐⭐⭐ | 隐式参考的触发机制 | Agent 怎么判断该查文件 |
| ⭐⭐⭐ | 文件摘要提取（入库时生成） | 支撑隐式参考 |
| ⭐⭐ | 生命周期管理实现 | temp 清理 + persistent 管理 |
| ⭐ | 多用户 file_id 归属校验 | 安全隔离 |

---

## 九、设计决策记录（ADR）

### ADR-001：为什么选 Tool 方案而非前置管线

**背景**：文件处理最初设计为"收到文件就塞 prompt"的前置管线。

**决策**：改为 file_id + Tool 管理方案。

**理由**：
1. 前置管线中 LLM 不知道文件存在（匿名文本），滚出窗口即丢失
2. Tool 方案中 file_id 是对话历史的一部分，Agent 可反复查询
3. 支持隐式参考（Agent 主动关联历史文件）
4. 主流 Agent（Claude/Cursor/WorkBuddy）均采用此模式

**代价**：多一次 LLM 决策延迟；Tool 可能选错。

**适用条件**：长期对话、多文件、需要 Agent 自主性的场景。纯临时对话可降级为前置管线。

---

## 十、一句话收口

> 文件不是输入数据，是 Agent 可管理的资源。四层架构：入口硬路由检测 → 文件处理管线（Loader + 大小分流 + 摘要）→ 语义路由 + Tool 管理（Agent 主动决策）→ 生命周期后台管理。当前选定 Tool 方案，支持长对话和隐式参考。